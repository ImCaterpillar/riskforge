# riskforge 依赖说明
#
# 核心运行时（数据层 / 回测引擎 / 风险指标 / CLI / 单元测试）仅使用 Python 3.10+ 标准库，
# 不依赖任何第三方包，因此在隔离、无网络环境下也能直接构建与运行测试：
#     python -m unittest discover -s tests
#
# 本文件存在是为了显式声明依赖策略，核心 dependencies 为空。
#
# 可选依赖（仅图表/HTML 报告功能需要，联网环境按需自行安装，版本约束与 pyproject 一致）：
#   matplotlib>=3.7   # 净值曲线/回撤图等可视化报告
#
# 安全约定：不得在本文件或仓库任何位置写入令牌、私有源地址、客户/内网信息。
